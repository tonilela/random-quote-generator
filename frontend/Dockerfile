FROM node:20

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

ARG REACT_APP_BACKEND_URL
ENV REACT_APP_BACKEND_URL=${REACT_APP_BACKEND_URL}

ARG REACT_APP_FRONTEND_PORT
ENV PORT=${REACT_APP_FRONTEND_PORT}

EXPOSE ${REACT_APP_FRONTEND_PORT}

CMD ["npm", "start"]
